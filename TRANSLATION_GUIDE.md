# Руководство по многоязычной системе

## Что уже реализовано

✅ **Файл translations.js** - содержит переводы на 5 языков:
- English (en-US)
- Русский (ru-RU)
- Español (es-ES)
- Français (fr-FR)
- Deutsch (de-DE)

✅ **Языковый переключатель** - добавлен в навигацию (правый верхний угол)

✅ **Функции смены языка** - автоматическое переключение контента

✅ **Data-атрибуты** - добавлены к ключевым элементам:
- Навигация
- Hero секция
- Floating badges

## Как добавить переводы к остальным элементам

### Шаг 1: Добавить data-атрибут к HTML элементу

Для каждого текстового элемента, который нужно перевести, добавьте атрибут `data-i18n`:

```html
<!-- Пример -->
<h2 class="section__title" data-i18n="features.title">
    Почему выбирают File Converter Pro?
</h2>
```

Значение атрибута `data-i18n` - это путь к переводу в объекте `translations.js`.

### Шаг 2: Добавить перевод в translations.js

Откройте `assets/js/translations.js` и добавьте перевод для каждого языка:

```javascript
'en-US': {
    features: {
        title: 'Why choose File Converter Pro?',
        subtitle: 'Everything you need for file conversion in one application',
        // ...
    }
}
```

## Структура переводов

Переводы организованы по секциям:

```javascript
{
    nav: { ... },           // Навигация
    hero: { ... },          // Главная секция
    features: { ... },      // Возможности
    formats: { ... },       // Форматы
    video: { ... },         // Видео секция
    howItWorks: { ... },    // Как это работает
    pricing: { ... },       // Цены
    faq: { ... },          // FAQ
    cta: { ... },          // Call to Action
    footer: { ... }        // Подвал
}
```

## Примеры использования

### Простой текст
```html
<p data-i18n="hero.subtitle">Текст для перевода</p>
```

### Вложенные объекты
```html
<span data-i18n="hero.stats.formats">Форматов</span>
```

### Кнопки
```html
<a href="..." data-i18n="hero.downloadBtn">Скачать сейчас</a>
```

### Списки
```html
<li data-i18n="pricing.features.0">32+ supported formats</li>
```

## Добавление нового языка

1. Откройте `assets/js/translations.js`
2. Добавьте новый объект языка (например, для итальянского):

```javascript
'it-IT': {
    nav: {
        features: 'Caratteristiche',
        formats: 'Formati',
        // ...
    },
    // ... остальные секции
}
```

3. Добавьте кнопку языка в навигацию (`index.html`):

```html
<button class="language-option" data-lang="it-IT">🇮🇹 Italiano</button>
```

4. Обновите функцию `getBrowserLanguage()` в `translations.js`:

```javascript
function getBrowserLanguage() {
    const lang = navigator.language || navigator.userLanguage;
    if (lang.startsWith('ru')) return 'ru-RU';
    if (lang.startsWith('es')) return 'es-ES';
    if (lang.startsWith('fr')) return 'fr-FR';
    if (lang.startsWith('de')) return 'de-DE';
    if (lang.startsWith('it')) return 'it-IT'; // Новый язык
    return 'en-US';
}
```

5. Добавьте код языка в `applyTranslations()` в `script.js`:

```javascript
const langCodes = {
    'en-US': 'EN',
    'ru-RU': 'RU',
    'es-ES': 'ES',
    'fr-FR': 'FR',
    'de-DE': 'DE',
    'it-IT': 'IT' // Новый язык
};
```

## Секции, которые нужно перевести

Вам нужно добавить data-атрибуты к следующим секциям:

- [ ] Features Section (#features)
- [ ] Formats Section (#formats)
- [ ] Video Section (#video)
- [ ] How It Works Section
- [ ] Pricing Section (#pricing)
- [ ] FAQ Section
- [ ] CTA Section
- [ ] Footer

Следуйте примерам выше для каждой секции.

## Тестирование

1. Откройте `index.html` в браузере
2. Кликните на языковый переключатель в правом верхнем углу
3. Выберите язык
4. Убедитесь, что весь контент переведен

## Советы

- Используйте понятные ключи для переводов (например, `features.title` вместо `f1`)
- Группируйте переводы по секциям для лучшей организации
- Сохраняйте одинаковую структуру для всех языков
- Проверяйте длину переведенного текста - он может не помещаться в дизайн

## Поддержка

Если у вас возникли вопросы, изучите:
- `assets/js/translations.js` - структура переводов
- `assets/js/script.js` - функции смены языка
- `index.html` - примеры использования data-атрибутов
